import type { Metadata } from "next";

export const metadata: Metadata = { title: "Contact Us | Nuref" };

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
