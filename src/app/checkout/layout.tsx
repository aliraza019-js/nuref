import type { Metadata } from "next";

export const metadata: Metadata = { title: "Checkout | Nuref" };

export default function CheckoutLayout({ children }: { children: React.ReactNode }) {
  return children;
}
